## Gemini Added Memories
- For UI state management, use a pattern where the `UiState` data class holds both the state data and the interaction functions (callbacks like `onClick`, `onValueChange`, etc.) to make state handling easier.

## Update for hilt
- `androidx.hilt.lifecycle.viewmodel.compose.hiltViewModel` is deprecated. Moved to package: `androidx.hilt.lifecycle.viewmodel.compose.hiltViewModel`